The skill can use local writing samples or user-provided exports to build a compact voice profile.

The skill includes a lightweight fact-check pass for claim-like sentences.

It does not automatically analyze social media history unless the user provides exports or explicitly connects a source through a host agent.
